const homeController = require('./controller-home');
const pupukController = require('./controller-pupuk');
const bibitController = require('./controller-bibit');
const loginController = require('./controller-login');
const registerController = require('./controller-register');

module.exports = {
    homeController,
    pupukController,
    bibitController,
    loginController,
    registerController
};